1. To start the project, run `npm install` to install all dependencies.
2. After the installation create a `.env` file in the root directory and add the following environment variables:
```
VITE_SERVER_URL=http://localhost:3000
```
3. To start the project, run `npm run dev`. The application will be available at `http://localhost:5173`.